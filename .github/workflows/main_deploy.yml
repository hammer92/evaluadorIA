name: Deploy Full Firebase Stack

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Firebase
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # --- BLOQUE FRONTEND (WEB) ---
      - name: Install & Build Web
        run: pnpm build
        env:
          # Inyectar config de Firebase para el build de la web
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}

      # --- BLOQUE DESPLIEGUE FINAL ---
      # firebase-tools v13+ auto-carga archivos .env* del directorio `source`
      # de las CFs (apps/functions, ver `firebase.json`) y los inyecta como
      # runtime env vars de las Cloud Functions v2 (leidas via process.env.X).
      # El proyecto solo tiene un alias (`default` en .firebaserc), por lo que
      # alcanza con `apps/functions/.env`. El archivo es gitignored y se
      # reconstruye aqui en cada deploy a partir de GitHub Secrets.
      - name: Build apps/functions/.env from secrets
        working-directory: apps/functions
        run: |
          {
            printf 'SESSION_COOKIE_SECRET=%s\n' "$SESSION_COOKIE_SECRET"
            printf 'ALLOWED_ORIGINS=%s\n'      "$ALLOWED_ORIGINS"
            printf 'REPOSITORY_DRIVER=firebase\n'
            printf 'FIREBASE_ADMIN_PROJECT_ID=%s\n' "$FIREBASE_ADMIN_PROJECT_ID"
            printf 'OPENAI_API_KEY=%s\n'       "$OPENAI_API_KEY"
          } > .env
        env:
          SESSION_COOKIE_SECRET: ${{ secrets.SESSION_COOKIE_SECRET }}
          ALLOWED_ORIGINS: ${{ secrets.ALLOWED_ORIGINS }}
          FIREBASE_ADMIN_PROJECT_ID: ${{ secrets.FIREBASE_ADMIN_PROJECT_ID }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Deploy static + rules (Hosting + Firestore + Storage)
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting,firestore,storage --non-interactive
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          PROJECT_ID: agente-entrevistador-ia

      - name: Deploy functions (auto-loads apps/functions/.env)
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only functions --non-interactive
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          PROJECT_ID: agente-entrevistador-ia

      - name: Post-deploy smoke test (Hosting URL reachable)
        if: success()
        run: |
          URL="https://agente-entrevistador-ia.web.app"
          echo "Smoke-testing $URL ..."
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 30 "$URL")
          if [ "$HTTP_STATUS" -lt 200 ] || [ "$HTTP_STATUS" -ge 400 ]; then
            echo "::error::Smoke test failed for $URL (HTTP $HTTP_STATUS)"
            exit 1
          fi
          echo "Smoke test OK ($URL → HTTP $HTTP_STATUS)"
